package fuscia:task@0.1.0;

interface task {
  record context {
    execution-id: string,
    node-id: string,
    task-id: string,
  }

  record output {
    data: string,
  }

  execute: func(ctx: context, data: string) -> result<output, string>;
}
