package fuschia:platform@0.1.0;

/// Base platform world with common imports for all components.
/// Note: HTTP support (wasi:http/outgoing-handler) will be added later
/// once WASI integration is complete.
world platform {
  import fuschia:kv/kv@0.1.0;
  import fuschia:config/config@0.1.0;
  import fuschia:log/log@0.1.0;
}

/// World for task components.
/// Tasks execute work within a workflow node.
world task-component {
  include platform;
  export fuschia:task/task@0.1.0;
}

/// World for trigger components.
/// Triggers initiate workflow execution based on events.
world trigger-component {
  include platform;
  export fuschia:trigger/trigger@0.1.0;
}
