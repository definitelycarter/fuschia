[package]
name = "fuschia-host-kv"
edition.workspace = true
version.workspace = true
description = "Key-value store trait and in-memory implementation for fuschia host capabilities"

[dependencies]

[dev-dependencies]
tokio = { version = "1", features = ["rt", "macros"] }
