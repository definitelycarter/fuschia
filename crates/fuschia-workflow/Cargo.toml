[package]
name = "fuschia-workflow"
edition.workspace = true
version.workspace = true
description = "Locked/resolved workflow representation for Fuscia"

[dependencies]
fuschia-config = { path = "../fuschia-config" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
